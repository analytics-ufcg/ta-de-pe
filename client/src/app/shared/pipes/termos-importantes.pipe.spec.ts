import { TermosImportantesPipe } from './termos-importantes.pipe';

describe('TermosImportantesPipe', () => {
  it('create an instance', () => {
    const pipe = new TermosImportantesPipe();
    expect(pipe).toBeTruthy();
  });
});
