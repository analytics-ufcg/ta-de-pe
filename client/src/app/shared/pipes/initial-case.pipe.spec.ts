import { InitialCasePipe } from './initial-case.pipe';

describe('InitialCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialCasePipe();
    expect(pipe).toBeTruthy();
  });
});
