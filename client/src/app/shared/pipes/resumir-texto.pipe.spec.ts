import { ResumirTextoPipe } from './resumir-texto.pipe';

describe('ResumirTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumirTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
